<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="board">

	<select id="selectAllBoard" resultType="DTO" parameterType="DTO">
		select
			bc,
			title,
			contentt,
			createdate,
			person,
			categoryy,
			countt
		from
			hyesung
		order by 
			bc
	</select>

</mapper>